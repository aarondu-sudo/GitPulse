# GitHubStar 设计文档

## 项目概述

GitHubStar 是一个展示 GitHub 最近热门项目的静态网站，帮助开发者快速发现社区中值得关注的项目。

## 功能需求

### 核心功能
- 展示多维度热门项目：Trending 排行、Star 增长最快、按语言分类
- 完整项目信息：基本数据、统计数据、编程语言、更新时间
- 筛选搜索：按时间周期（日/周/月）、编程语言、关键词搜索
- 项目详情跳转：点击卡片直接跳转到 GitHub 页面

## 技术方案

### 整体架构

```
githubstar/
├── index.html          # 入口页面
├── src/
│   ├── api/           # API 调用模块
│   │   └── github.js  # 封装第三方 API 请求
│   ├── ui/            # UI 渲染模块
│   │   ├── card.js    # 项目卡片渲染
│   │   └── filters.js # 筛选器组件
│   ├── state/         # 状态管理
│   │   └── store.js   # 简单的状态管理（当前筛选条件、项目列表）
│   ├── app.js         # 主入口
│   └── styles.css     # 样式文件
├── vite.config.js     # Vite 配置
└── package.json
```

### 技术选型
- **构建工具**: Vite - 快速开发、热更新、生产优化
- **样式**: 原生 CSS + CSS Variables - 无框架依赖、轻量
- **数据获取**: fetch API 调用第三方趋势 API
- **状态管理**: 简单对象 + localStorage 持久化筛选条件
- **部署**: GitHub Pages / Netlify / Vercel（免费静态托管）

## 数据源与降级策略

### 多数据源配置

```javascript
const API_SOURCES = [
  // 主选：GitHub Trending API（第三方）
  {
    name: 'gh-trending-api',
    url: 'https://gh-trending-api.herokuapp.com/repositories',
    parser: 'json'
  },
  // 备选1：GitHub 官方 Search API
  {
    name: 'github-search',
    url: 'https://api.github.com/search/repositories',
    parser: 'github',
    requiresToken: false
  },
  // 备选2：另一个第三方 API
  {
    name: 'trending-repos',
    url: 'https://trending-git.vercel.app/api',
    parser: 'json'
  }
];
```

### 自动降级逻辑

数据源按优先级尝试，失败自动切换下一个源，确保服务可用性。

| 场景 | 降级方案 |
|------|---------|
| API 宕机 | 自动切换到下一个源 |
| API 停止维护 | 代码中预留多个源，可配置切换 |
| GitHub API 限流 | 使用无需 token 的公开端点 |

## UI 设计

### 页面布局

```
┌─────────────────────────────────────────────┐
│  Header: GitHubStar  + 简单 Logo              │
├─────────────────────────────────────────────┤
│  Filter Bar                                  │
│  [日/周/月] [语言▼] [搜索框____] [搜索]       │
├─────────────────────────────────────────────┤
│  Loading / Error / Results Count             │
├─────────────────────────────────────────────┤
│  项目卡片网格 (4列桌面 / 2-3列平板 / 1列手机) │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │
│  │ 卡片1 │ │ 卡片2 │ │ 卡片3 │ │ 卡片4 │        │
│  └──────┘ └──────┘ └──────┘ └──────┘        │
├─────────────────────────────────────────────┤
│  Footer: 数据来源说明                        │
└─────────────────────────────────────────────┘
```

### 项目卡片设计

```
┌──────────────────────────────────────┐
│ 🐦 repo-owner/name          ⭐ 12.5k  │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                      │
│ 项目描述简介，展示项目的主要功能和    │
│ 用途，最多显示 2-3 行...             │
│                                      │
│ 🟢 Python  🍴 1.2k  👁️ 350  📅 2天前 │
│              [→ 查看详情]             │
└──────────────────────────────────────┘
```

## 筛选与搜索

### 筛选器配置

```javascript
const FILTERS = {
  period: [
    { value: 'daily', label: '今日热门' },
    { value: 'weekly', label: '本周热门' },
    { value: 'monthly', label: '本月热门' }
  ],
  language: [
    { value: '', label: '全部语言' },
    { value: 'python', label: 'Python' },
    { value: 'javascript', label: 'JavaScript' },
    { value: 'typescript', label: 'TypeScript' },
    { value: 'go', label: 'Go' },
    { value: 'rust', label: 'Rust' },
    { value: 'java', label: 'Java' },
    { value: 'cpp', label: 'C++' }
  ],
  sort: [
    { value: 'trending', label: '趋势' },
    { value: 'stars', label: 'Star 增长' },
    { value: 'forks', label: 'Fork 数' }
  ]
};
```

### URL 状态同步

筛选条件反映在 URL 中，支持分享：
```
?period=weekly&language=python&q=ai
```

## 数据模型

### 统一数据格式

```javascript
const Repository = {
  id: 'string',           // 唯一标识
  name: 'string',         // owner/repo
  owner: 'string',        // 所有者
  description: 'string',  // 描述
  language: 'string',     // 主要语言
  stars: number,          // Star 数
  forks: number,          // Fork 数
  watchers: number,       // Watch 数
  starsToday: number,     // 今日新增 Star
  starsWeekly: number,    // 本周新增 Star
  updatedAt: 'string',    // 更新时间
  url: 'string',          // GitHub 链接
  avatar: 'string'        // 所有者头像
};
```

### API 封装接口

```javascript
class GitHubTrendingAPI {
  async fetchTrending({ period, language, sort });
  async fetchByStarGrowth({ period, language });
  async search({ query, language });
}
```

## 错误处理

| 错误类型 | 处理方式 |
|---------|---------|
| API 超时 | 自动重试 2 次，显示重试按钮 |
| 所有源失败 | 显示友好错误 + 降级提示 |
| 网络断开 | 检测 navigator.onLine，提示连接 |
| 限流 429 | 延迟重试，显示"稍后再试" |

### 状态视图

```javascript
const VIEW_STATES = {
  LOADING: '<div class="spinner">加载中...</div>',
  EMPTY: '<div class="empty">暂无相关项目</div>',
  ERROR: '<div class="error">加载失败，<button onclick="retry()">重试</button></div>'
};
```

## 性能优化

- **卡片懒加载**: 使用 IntersectionObserver 按需渲染
- **请求缓存**: 相同筛选条件 5 分钟内不重复请求
- **骨架屏**: 首次加载显示占位，提升感知速度

## 部署方案

- **托管**: GitHub Pages / Netlify / Vercel（免费）
- **域名**: 默认域名或自定义域名
- **构建**: `vite build` 生成静态文件
